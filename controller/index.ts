import { handleFilters } from "./handleFilters";
import { handleSearch } from "./handleSearch";
import { handleSortByString } from "./handleSortByString";
import { handleSortByNumber } from "./handleSortByNumber";
export const handler = {
  handleFilters,
  handleSearch,
  handleSortByString,
  handleSortByNumber
};
